#include <bits/stdc++.h>
using namespace std;
#define fast  ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0)
#define nl "\n"

void solve(){
   string s="22";
   
    int n = pow(10,s.size());
    cout<<5*pow(10 , (s.size() - 1))<<endl;
    cout<<n;
}
int main () {

        fast;
        solve();
        return 0;
   
}

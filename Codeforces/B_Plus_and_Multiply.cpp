#include <bits/stdc++.h>
using namespace std;
#define fast                      \
    ios_base::sync_with_stdio(0); \
    cin.tie(0);                   \
    cout.tie(0)
#define testcase \
    int t;       \
    cin >> t;    \
    while (t--)
#define nl "\n"

void solve()
{

    testcase
    {
        int n, a, b, x1 = 0, y1 = 0, x2 = 0, y2 = 0;
        bool flg = false;
        cin >> a >> b >> n;
        while (true)
        {
            
        }
        if (flg)
            cout << "Yes" << nl;
        else
            cout << "No" << nl;
    }
}
int main()
{

    fast;
    solve();
    return 0;
}

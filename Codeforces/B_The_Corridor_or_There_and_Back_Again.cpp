#include <bits/stdc++.h>
using namespace std;
#define fast                      \
    ios_base::sync_with_stdio(0); \
    cin.tie(0);                   \
    cout.tie(0)
#define testcase \
    int t;       \
    cin >> t;    \
    while (t--)
#define nl "\n"

void solve()
{
    testcase
    {
        int n, a, b, cnt = 0, x, y;
        cin >> n;
        ans = INT_MAX;
        while (n--)
        {
            cin>>a[]
        }
        cout << cnt << nl;
    }
}
int main()
{

    fast;
    solve();
    return 0;
}

#include <bits/stdc++.h>
using namespace std;
#define fast  ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0)
#define nl "\n"

void solve(){
   int t;
   cin>>t;
   while (t--)
   {
        string s;
        cin>>s;
        string ans="";
        for (int i = 0; i <s.size()/2; i++)
        {
            if(s[i+1]==')'&& s[i]=='(')
            
        }
        
   }
    
}
int main () {

        fast;
        solve();
        return 0;
   
}
